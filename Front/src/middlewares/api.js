/* Package imports */
import axios from 'axios';

/* Local imports */
import { apiUrl } from './url';

// Action


/* Paws API MiddleWare */
const Api = (store) => (next) => (action) => {

};

export default Api;