/* Actions */

/* Initial State user infos */
const initialState = {
  
};

/* Api Paws reducer */

const Reducer = (oldState = initialState, action = {}) => {

  switch (action.type) {

    default:
      return {...oldState}
  }
};

export default Reducer;